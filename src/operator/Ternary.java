package operator;

public class Ternary {
    public static void main(String[] args) {
        int age = 19;
        String result = age >18 ? "Allowed to vote":"not Allowed";
        System.out.println(result);
    }
}
